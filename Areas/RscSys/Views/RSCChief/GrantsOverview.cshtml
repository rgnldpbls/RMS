@model IEnumerable<rscSys_final.Models.GrantsOverviewViewModel>
@{
    ViewData["Title"] = "Grants Overview";
    Layout = "_LayoutCPages";
}

<h1><strong>Number of Grant Endorsed per College</strong></h1>

<div class="mb-4">
    <canvas id="approvedGrantsBarChart"></canvas>
</div>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>College</th>
            <th>Number of Grant Endorsed</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Any())
        {
            foreach (var item in Model)
            {
                <tr>
                    <td>@item.College</td>
                    <td>@item.NumberOfApprovedGrants</td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="2" class="text-center">No data available</td>
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            var ctx = document.getElementById('approvedGrantsBarChart').getContext('2d');

            var colleges = @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(Model.Select(m => m.College)));
            var numberOfGrants = @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(Model.Select(m => m.NumberOfApprovedGrants)));

            var approvedGrantsBarChart = new Chart(ctx, {
                type: 'bar',
                data: {
                    labels: colleges,
                    datasets: [{
                        label: 'Number of Approved Grants',
                        data: numberOfGrants,
                        backgroundColor: 'rgba(133, 0, 0, 0.8)',
                        borderColor: 'rgba(133, 0, 0, 1)',
                        borderWidth: 1
                    }]
                },
                options: {
                    responsive: true,
                    scales: {
                        y: {
                            beginAtZero: true,
                            title: {
                                display: false,
                                text: 'Number of Approved Grants'
                            }
                        }
                    }
                }
            });
        });
    </script>
}