@model IEnumerable<rscSys_final.Models.IncentivesOverviewViewModel>
@{
    ViewData["Title"] = "Incentives Overview";
    Layout = "_LayoutCPages";
}

<h1><strong>Number of Incentive Endorsed per College</strong></h1>

<div class="mb-4">
    <canvas id="approvedIncentivesBarChart"></canvas>
</div>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>College</th>
            <th>Number of Incentive Endorsed</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Any())
        {
            foreach (var item in Model)
            {
                <tr>
                    <td>@item.College</td>
                    <td>@item.NumberOfApprovedIncentives</td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="2" class="text-center">No data available</td>
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            var ctx = document.getElementById('approvedIncentivesBarChart').getContext('2d');

            var colleges = @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(Model.Select(m => m.College)));
            var numberOfIncentives = @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(Model.Select(m => m.NumberOfApprovedIncentives)));

            var approvedIncentivesBarChart = new Chart(ctx, {
                type: 'bar',
                data: {
                    labels: colleges,
                    datasets: [{
                        label: 'Number of Approved Incentives',
                        data: numberOfIncentives,
                        backgroundColor: 'rgba(255, 193, 7, 0.8)',
                        borderColor: 'rgba(255, 193, 7, 1)',
                        borderWidth: 1
                    }]
                },
                options: {
                    responsive: true,
                    scales: {
                        y: {
                            beginAtZero: true,
                            title: {
                                display: false,
                                text: 'Number of Approved Incentives'
                            }
                        }
                    }
                }
            });
        });
    </script>
}